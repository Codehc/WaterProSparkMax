/*const int RELAY_PIN = D5;

void setup() {
  pinMode(RELAY_PIN, OUTPUT);
}

byte state = HIGH;
void loop() {
  digitalWrite(RELAY_PIN, state);
  
  if (state == HIGH) {
    state = LOW;
  } else if (state == LOW) {
    state = HIGH;
  }
  
  delay(1000);
}

*/